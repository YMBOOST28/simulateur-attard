
  # Landing page with simulator

  This is a code bundle for Landing page with simulator. The original project is available at https://www.figma.com/design/Xdjiyf69ldo7vjeHV6WWA5/Landing-page-with-simulator.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  